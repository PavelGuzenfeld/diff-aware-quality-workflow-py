name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: ./.github/workflows/auto-release.yml
    with:
      enable_provenance: true
